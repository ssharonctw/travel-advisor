import  React from'react';
import ReactDOM from 'react-dom';

import App from './App';

//in every react app, we need to mount application to root div
ReactDOM.render(<App />, document.getElementById('root'));